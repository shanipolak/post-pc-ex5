<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="il.ac.huji.todolist"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    
    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" 
        android:debuggable="true" >
        <activity
            android:name="il.ac.huji.todolist.TodoListManagerActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="il.ac.huji.todolist.AddNewTodoItemActivity"
            android:label="@string/title_activity_add_new_todo_item"
            android:theme="@android:style/Theme.Dialog" > 
        </activity>
         <activity
            android:name="il.ac.huji.todolist.SuggestTweetsTodos"
            android:theme="@android:style/Theme.Dialog" >
        </activity>
         <activity    
             android:name="il.ac.huji.todolist.TwitterHashtagPreference"
             android:theme="@android:style/Theme.Black.NoTitleBar" > 
         </activity> 
    </application>
     

</manifest>